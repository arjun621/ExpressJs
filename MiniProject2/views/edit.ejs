<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
    <link rel="stylesheet" href="/stylesheets/edit.css">
</head>
<body>

    <div class="nav">
        <a href="/profile" class="back">← Back to Profile</a>
    </div>

    <div class="container">
        <h1>Edit Task</h1>

        <form action="/update/<%= task._id %>" method="POST" class="edit-task-form">
            <label for="title">Task Title</label>
            <input type="text" id="title" name="title" value=<%= task.title %> required>

            <label for="description">Task Description</label>
            <textarea id="description" name="description" rows="5" required><%= task.description %></textarea>

            <button type="submit" class="submit-btn">Update Task</button>
        </form>
    </div>

</body>
</html>